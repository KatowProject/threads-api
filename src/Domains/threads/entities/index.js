const CreatedThread = require('./CreatedThread');
const CreateThread = require('./CreateThread');
const ThreadDetail = require('./DetailThread');

module.exports = { CreatedThread, CreateThread, ThreadDetail };